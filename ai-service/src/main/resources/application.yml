server:
  port: 8083

spring:
  application:
    name: AI-SERVICE
groq:
  api-key: ${GROQ_API_KEY}
eureka:
  instance:
    hostname: ${AI_SERVICE_HOSTNAME:localhost}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}